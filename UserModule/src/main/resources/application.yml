spring:
  application:
    name: UerModule
  datasource:
    url: jdbc:mysql://localhost:3306/online_ordering_system?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 135421
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 10000
mybatis:
  mapper-locations: classpath:mapper/*.xml